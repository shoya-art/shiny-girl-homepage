# Shiny Girl ホームページ — ローカル確認方法

## 開き方

1. 同じフォルダにある `ローカル確認.command` をダブルクリックします。
2. ターミナル画面が開き、少し待つとブラウザに3つのデザイン案が表示されます。
3. 確認中はターミナル画面を開いたままにしてください。
4. 終了するときは、ターミナル画面で `Control + C` を押します。

クリックすると、その時点の修正内容から確認版を作るため、ブラウザが開くまで数秒かかります。

## 今後の更新方法

- 通常の修正・確認は、このローカル確認版で行います。
- 修正後に確認し直す場合は、いったん `Control + C` で終了し、`ローカル確認.command` をもう一度ダブルクリックします。
- HTMLの内容に相当するページ構成は `app/`、画像は `public/` に保存されています。
- ローカルで内容が確定するまでは、GitHubへのコミット・プッシュは不要です。
- 先方へ共有する段階になったときだけ、確定版をGitHubへ反映します。

## macOSで警告が出た場合

初回に「開発元を確認できないため開けません」と表示された場合は、`ローカル確認.command` を右クリックして「開く」を選び、もう一度「開く」を押してください。
